# exportMysql の既定設定。プロジェクト側 config で上書きする。
#
# host_group : 接続先 DB ホストを解決する inventory グループ名 (末尾 group で許可)
# port       : MySQL ポート
# database   : 対象データベース名
# user       : 接続ユーザー (パスワードは secret {host}-{user} で解決)
# tables     : エクスポート対象テーブル名のリスト
#
# 接続情報 (ホスト・パスワード) を config に直書きしてはならない (禁止契約)。
stfw:
  process:
    exportMysql:
      host_group: ""
      port: "3306"
      database: ""
      user: ""
      tables: []
