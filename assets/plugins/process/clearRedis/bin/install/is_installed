#!/bin/bash
# 前提コマンド (redis-cli) が揃っていればインストール済みとみなす。
for cmd in redis-cli; do
  if ! command -v "${cmd}" >/dev/null 2>&1; then
    echo "${cmd} is not installed."
    exit 1
  fi
done
echo "true"
exit 0
