# Arrange: 口座マスタ (初期残高) を投入する。入力 CSV: data/appdb/accounts.csv。
# 接続系は config/plugins/process/importPostgres/config.yml で共通化 (ここは tables のみ)。
stfw:
  process:
    importPostgres:
      tables:
        - accounts
