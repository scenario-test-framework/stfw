# Collect: 取引反映後の残高を CSV エビデンス化する。出力: evidence/appdb/accounts.csv。
# 接続系は config/plugins/process/exportPostgres/config.yml で共通化 (ここは tables のみ)。
stfw:
  process:
    exportPostgres:
      tables:
        - accounts
