# updateBizdate の接続系をプロジェクト全体で共通化する上書き設定 (§8.1 の上書きチェーン)。
# 各シナリオのプロセス config/config.yml には設定キーが不要になる
# (更新値は stfw_bizdate・対象テーブルは biz_calendar 固定)。
# database / user は stfw.yml (stfw.db.*) を単一ソースとし、${stfw_db_database} / ${stfw_db_user}
# で参照する (run 開始時に stfw.yml が環境へ export される。AS-BUILT §8.2)。
stfw:
  process:
    updateBizdate:
      host_group: db
      port: "5432"
      database: ${stfw_db_database}
      user: ${stfw_db_user}
