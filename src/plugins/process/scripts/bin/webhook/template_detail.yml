#payload:
# run:
#   scenario:
#     bizdate:
#       process:
#         plugin:
#           type: scripts
#           targets:
              - ${script_name}:
                  result: "${script_result}"
                  start time: "${script_start_time}"
                  end time: "${script_end_time}"
                  processing time: "${script_processing_time}"
