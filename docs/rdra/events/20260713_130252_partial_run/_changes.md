# 変更サマリ

- event_id: 20260713_130252_partial_run
- 元USDM: 20260713_130252_partial_run
- 生成日時: 2026-07-13T13:12:22

## 追加

- バリエーション: 部分実行指定（REQ-025/SPEC-025-01/02。値: --from {bizdate_dir}[/{process_dir}]（再開）、--only {bizdate_dir}[/{process_dir}]（ピンポイント実行）、指定なし（全体実行）。--from は指定ノードより前をスキップして最後まで実行、--only は指定ノードのサブツリーのみ実行）
- 条件: 部分実行の指定制約（REQ-025/SPEC-025-03。指定パスはシナリオ相対・ディレクトリ名完全一致、--from / --only 排他、空値明示指定はエラー、部分実行時のシナリオ指定は 1 つのみ、--dry-run 併用可、存在しないノードは run_id 採番・ジャーナル作成・ハウスキープ前に fail-fast、静的検証はフィルタ前の全体に実施。バリエーション「部分実行指定」参照）
- 条件: 部分実行のスキップ意味論（REQ-025/SPEC-025-04。スキップされたノードはジャーナルへ一切記録されない（node_start / node_end もフックも無い）、祖先階層のフック（run / scenario / 実行対象を含む bizdate の setup・teardown）は通常どおり実行、run node_start の attrs に部分実行時のみ from / only を記録、スキップした副作用の再現はユーザー責務。バリエーション「部分実行指定」参照）

## 変更

- 条件: run 実行の前提条件 → 部分実行（--from / --only）時は指定ノードが存在すること（存在しない場合は run_id 採番・ジャーナル作成・ハウスキープの前に fail-fast でエラー終了）を追記し、バリエーション「部分実行指定」を参照に追加（REQ-025/SPEC-025-03）
- 条件: run 前静的検証 → 部分実行時もフィルタ前のシナリオ全体を検証対象とする（実行範囲の絞り込みは検証範囲を狭めない）旨を追記し、バリエーション「部分実行指定」を参照に追加（REQ-025/SPEC-025-03）
- 条件: 実行結果ハウスキープ → 部分実行で指定ノードが存在しない場合は fail-fast が先行しハウスキープは実行されない旨を追記し、バリエーション「部分実行指定」を参照に追加（REQ-025/SPEC-025-03）
- 情報: 実行（run） → 属性に部分実行指定（--from / --only の指定パス）を追加、バリエーションに「部分実行指定」を追加。説明に --from（再開）/ --only（ピンポイント実行）の意味・排他・--dry-run 併用可・シナリオ 1 つのみ・存在しないノードの fail-fast・静的検証はフィルタ前全体を追記（REQ-025/SPEC-025-01/02/03/04）
- 情報: 実行ジャーナル（journal.jsonl） → 属性に run node_start の attrs（部分実行時のみ from / only（指定パス）を記録）を追加。説明にスキップされたノードは一切記録されない（node_start / node_end もフックも無い）・祖先階層のフックは通常どおり記録・指定なしの run では from / only は記録されない旨を追記（REQ-025/SPEC-025-04）
- アクター: テスト実行者 → 部分実行オプション（--from による失敗 run の途中からの再開 / --only による特定ノードのピンポイント実行）により全体を先頭から再実行せずにやり直し・動作確認ができる（スキップした副作用の再現はユーザー責務）旨を役割に追記（REQ-025/SPEC-025-01/02）
- BUC: シナリオ一括自動実行フロー → UC「シナリオを実行する」の説明に部分実行（--from / --only）の指定方法・制約・fail-fast・静的検証範囲を追記し、条件「部分実行の指定制約」を追加。UC「階層setup/teardownを実行する」の説明にスキップ意味論（スキップノードはジャーナル未記録・祖先フックは通常実行・run node_start attrs への from / only 記録）を追記し、条件「部分実行のスキップ意味論」を追加。UC「プロセスを実行する」にも条件「部分実行のスキップ意味論」を追加。受益者アクティビティに部分実行による再開・ピンポイント実行の便益を追記（REQ-025/SPEC-025-01〜04）
- BUC: 実行結果監視・確認フロー → アクティビティ「修正後のシナリオを再実行する」（UC「シナリオを実行する」）の説明を更新。旧記述「専用のリラン・途中再開I/Fは無く、実行のやり直しとなる」が REQ-025（--from による途中からの再開）と矛盾するため、部分実行 --from による途中再開を選択できる旨に改訂（affected_models 外だが、既存記述との矛盾解消のための整合性維持。REQ-025/SPEC-025-01）

## 削除

- なし
