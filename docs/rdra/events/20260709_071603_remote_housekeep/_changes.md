# 変更サマリ

- event_id: 20260709_071603_remote_housekeep
- 元USDM: 20260709_071603_remote_housekeep
- 生成日時: 2026-07-09T07:25:43

## 追加

- 条件: sshExec のリモートスクリプト一括実行（REQ-018/SPEC-018-01。scripts/ 配下を inventory グループ解決の各ホストへ ssh で送りファイル名昇順に一括実行、エラー時リターンコード 6 で即時終了。バリエーション「プロセスタイプ」参照）
- 条件: scpPut の原子的配置（REQ-018/SPEC-018-02。target/{グループ} 配下を inventory グループ解決の各ホストへ scp put、一時ディレクトリ経由 mv で原子的配置。バリエーション「プロセスタイプ」参照）
- 条件: 実行結果ハウスキープ（REQ-019/SPEC-019-01/02/03。stfw run 開始時に保存期間超過の実行ジャーナル・HTML レポートを物理削除、index 再生成。専用サブコマンド・常駐ジョブは設けない。バリエーション「実行結果保存期間設定」参照）
- バリエーション: 実行結果保存期間設定（REQ-019/SPEC-019-01。stfw.yml の stfw.housekeep.retention で指定する保存期間の区分。未設定時は削除しない）

## 変更

- BUC: シナリオ一括自動実行フロー → UC「シナリオを実行する」に run 開始時ハウスキープ（条件「実行結果ハウスキープ」・情報「実行ジャーナル（journal.jsonl）」「HTML レポート」）を追加。UC「プロセスを実行する」にリモートアクセス系プラグイン（条件「sshExec のリモートスクリプト一括実行」「scpPut の原子的配置」「プラグインのランタイム依存宣言と存在チェック」、イベント「リモートスクリプト一括実行（ssh）」「ローカルファイル原子的配置（scp）」）を追加し、プロセス実行の説明に組込みプラグイン群のリモートアクセス系を追記
- BUC: 実行結果監視・確認フロー → UC「HTMLレポートを生成する」に run 開始時ハウスキープによる HTML レポート物理削除・index 再生成（条件「実行結果ハウスキープ」）を追加
- アクター: テスト実行者 → run 開始時の実行結果自動ハウスキープにより継続利用でもストレージが逼迫しない旨を役割に追記
- 外部システム: テスト対象ホスト群 → リモートアクセス系（sshExec / scpPut）による ssh 一括実行・scp 原子的配置の対象であり、接続情報は inventory グループ名参照・secret 自動参照・ssh trust を再利用する旨を追記
- 情報: プラグイン → 組込みプラグイン群にリモートアクセス系（sshExec / scpPut）を追加、requires 例に ssh を追記
- 情報: Process / Plugin 設定（config.yml） → リモートアクセス系スキーマ（group / user / scpPut の配置先ディレクトリ、パスワードは secret 自動参照）を追加
- 情報: インベントリ → 接続先にリモートアクセス系（sshExec / scpPut）を追記
- 情報: パスワード → リモートアクセス系プラグインも {host}-{user} を自動参照する旨を追記
- 情報: SSH サーバキー（known_hosts） → リモートアクセス系（sshExec / scpPut）の ssh / scp 接続でも再利用される旨を追記
- 情報: プロジェクト設定（stfw.yml） → 属性に housekeep.retention（実行結果の保存期間）、バリエーションに「実行結果保存期間設定」を追加
- 情報: 実行ジャーナル（journal.jsonl） → 保存期間超過分が run 開始時ハウスキープで物理削除される旨を追記
- 情報: 実行（run） → run 開始時に保存期間超過の実行結果を自動ハウスキープしてから実行する旨を追記
- 情報: HTML レポート → 保存期間超過分（.stfw/reports/runs/{run_id}.html）の物理削除・index 再生成を属性と説明に追記
- 条件: プラグイン接続情報のグループ名参照 → リモートアクセス系（sshExec / scpPut）の指定方法（group / user / 配置先ディレクトリ）を追記
- 条件: プラグインのランタイム依存宣言と存在チェック → requires 例に ssh を追記し、リモートアクセス系は ssh / scp を宣言する旨を追記
- バリエーション: プロセスタイプ → 値に sshExec / scpPut を追加、組込みプラグイン群にリモートアクセス系を追記

## 削除

- なし
