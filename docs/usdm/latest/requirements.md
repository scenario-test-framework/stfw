# USDM 要求仕様書

- システム名: stfw
- イベントID: 20260707_233136_harvest_initial
- 作成日時: 2026-07-08T00:10:32
- ソース: 逆生成: /Users/suwa_sh/src/github.com/scenario-test-framework/stfw (commit ed02ba61, branch feature/kcov) を dist-harvest で解析

| ID | 要求 | 仕様 | 理由（背景） | 説明 |
|----|------|------|------------|------|
| REQ-001 | テスト担当者が、マルチプラットフォームバイナリ / Docker image / compose.yaml のいずれかの配布物とテンプレート初期化のみでシナリオテスト実行基盤（stfw プロジェクト）を各ホストに用意できる | | 導入の容易さが利用開始の前提であることは変わらない。v1.0 で Go 単一バイナリ化したことにより、依存モジュールのダウンロード（到達不能となっていた dl.bintray.com）や JVM / Python 2 / Ruby のランタイム準備が不要になり、導入の容易さをより確実に実現できるため | |
| SPEC-001-01 | | 配布方式をマルチプラットフォームバイナリ（GitHub Releases: linux/darwin × amd64/arm64 + windows/amd64）、Docker image（ghcr.io）、compose.yaml（stfw + nginx）に変更する。tar.gz 展開 + install スクリプト（dl.bintray.com からの依存ダウンロード）は廃止する。依存モジュール（digdag jar / Tukubai / Parsrs / yaml2json）は存在せず、JVM / Python 2 / Ruby は不要とする | | 1. Given GitHub Releases から対象プラットフォームのバイナリを取得済み When 実行パスに配置して stfw を実行する Then 追加のランタイム・依存モジュールなしに stfw コマンドが利用可能になる<br>2. Given ghcr.io の Docker image または compose.yaml When コンテナを起動する Then stfw コマンドが利用可能になる<br>3. Given v1.0 の配布物 When 導入手順を実施する Then install スクリプトの実行と依存モジュールのダウンロードは発生しない |
| SPEC-001-02 | | stfw init でテンプレート一式（stfw.yml / config / sample シナリオ）を展開してプロジェクトを開始できる。stfw.yml が既に存在するディレクトリでは再初期化をエラーとする（従来仕様を維持） | | 1. Given 空のプロジェクトディレクトリ When stfw init を実行する Then stfw.yml・config/・scenario/sample が生成される<br>2. Given stfw.yml が存在するディレクトリ When stfw init を実行する Then エラー終了する |
| SPEC-001-03 | | プロジェクト設定（stfw.yml）はデフォルト（stfw 本体の内蔵デフォルト）→ プロジェクトの順に読込・上書きされ、環境変数として全スクリプトへ公開される（従来の上書き順を維持。STFW_HOME/config は廃止）。stfw.yml の stfw.server.* 設定は廃止され、読み込み時に廃止警告を表示する | | 1. Given プロジェクトの stfw.yml でデフォルト設定値を上書きした状態 When 任意のコマンドを実行する Then プロジェクト側の値が環境変数として有効になる<br>2. Given stfw.server.* 設定を含む stfw.yml When 設定を読み込む Then 廃止警告が表示され、設定値は実行に影響しない |
| SPEC-001-04 | | STFW_HOME（配布ディレクトリ）概念を廃止する。プラグインに対する env 契約（stfw_* / STFW_PROJ_DIR 系の環境変数公開）は維持する | | 1. Given v1.0 のバイナリ When STFW_HOME を設定せずに任意のコマンドを実行する Then 正常に動作する<br>2. Given プラグインのスクリプト When プロセスを実行する Then stfw_* / STFW_PROJ_DIR 系の環境変数が従来どおり公開されている |
| REQ-002 | シナリオ作成者が、業務日付（bizdate）をまたぐ一連の業務処理を scenario > bizdate > process の 3 階層ディレクトリ構造とスクリプトとして記述し、静的検証（stfw validate）で実行可能性を事前確認できる | | 業務日付の進行を伴うバッチ処理系業務システムのシナリオテストでは、日付単位でテストを区切って進行させる必要がある。v1.0 ではワークフロー定義（dig）の生成・管理が不要になり、記述したディレクトリ構造そのものが実行定義となるため、検証は静的検証コマンドに分離した | |
| SPEC-002-01 | | scaffold 生成コマンドを stfw new scenario / stfw new bizdate / stfw new process に刷新する（旧 scenario -i / bizdate -i / process -i）。3 階層の scaffold（ディレクトリ・metadata.yml・config・scripts 雛形）生成と、bizdate は YYYYMMDD の 8 桁数字・seq は数値のみ・group は「_」を含まないことの検証は同一仕様で維持する | | 1. Given プロジェクト初期化済み When stfw new scenario / new bizdate / new process を順に実行する Then scenario/{name}/_{seq}_{bizdate}/_{seq}_{group}_{type}/ の階層が生成される<br>2. Given bizdate scaffold 生成 When bizdate に 8 桁数字以外を指定する Then エラー終了する |
| SPEC-002-02 | | プロセス配下の scripts/ に任意言語の実行可能ファイルをファイル名昇順 = 実行順で配置し、Process/Plugin 設定（config.yml）の値を環境変数として全スクリプトへ公開できる（組込み → プロジェクト → シナリオ内 Process 設定の順に上書き。従来仕様を維持） | | 1. Given config.yml に stfw.process.{type} 配下のキーを定義 When プロセスを実行する Then 全スクリプトから環境変数として参照できる |
| SPEC-002-03 | | ワークフロー定義（run.dig / scenario.dig / bizdate.dig）の生成を廃止し、静的検証を新コマンド stfw validate に分離する。validate はディレクトリ規約・プラグイン解決可否・config.yml を検証し、残存する *.dig ファイルには不要と警告する。run 開始前には validate 相当の静的検証が自動実行される | | 1. Given 3 階層のシナリオ構造 When stfw validate を実行する Then ディレクトリ規約・プラグイン解決可否・config.yml が検証され結果が表示される<br>2. Given 旧バージョンで生成した *.dig ファイルが残存 When stfw validate を実行する Then 不要である旨の警告が表示される<br>3. Given 規約違反のあるシナリオ構造 When stfw run を実行する Then run 開始前の自動静的検証でエラーとなり実行は開始されない |
| SPEC-002-04 | | dry-run は「execute / post_execute をスキップして実行する」の意味に整理する（stfw run --dry-run。setup → pre_execute → teardown は実行される） | | 1. Given シナリオ構造作成済み When stfw run --dry-run を実行する Then execute / post_execute は実行されず setup・pre_execute・teardown のみ実行される |
| REQ-003 | テスト実行者が、ディレクトリ構造で記述したシナリオテスト（複数シナリオ）を Go 単一バイナリに内蔵された逐次実行ランナーで一括自動実行でき、実行順序の保証とエラー時停止が担保される | | ディレクトリ構造どおりのワークフロー自動実行がフレームワークの中核目的であることは変わらない。v1.0 では外部ワークフローエンジン digdag を廃止して実行エンジンを内包したことで、server 起動などの前準備なしに 1 コマンドで実行でき、実行順序保証・エラー時停止・Blocked 伝播は内蔵ランナーが同一仕様で提供するため | |
| SPEC-003-01 | | stfw run <scenario-names...> で run_id（_{YYYYMMDDHHMMSS}_{PID}。従来形式を維持）を採番し、内蔵ランナーで実行を開始する。run 開始前に validate 相当の静的検証（対象シナリオの存在チェック等を統合）が自動実行される。digdag プロジェクトの push・attempt_id・「digdag server が起動中であること」の前提条件は廃止し、旧 server start → run -f の 2 段階を 1 コマンドに統合する | | 1. Given シナリオ存在 When stfw run を実行する Then 前準備コマンドなしに run_id が採番され実行が開始される<br>2. Given 対象シナリオが存在しない When stfw run を実行する Then run 開始前の静的検証でエラー終了する<br>3. Given v1.0 の stfw When 実行コンテキストを確認する Then attempt_id は存在せず run_id で実行が識別される |
| SPEC-003-02 | | 内蔵ランナーが run / scenario / bizdate 各階層の setup / teardown を直接実行し（digdag からの呼び戻しは廃止）、実行ジャーナルへ開始・終了イベントを記録して階層実行ステータス（Started → Success / Error）を確定させる | | 1. Given シナリオ実行中 When 全タスクが正常終了する Then 各階層の teardown で階層実行ステータスが Success になる<br>2. Given シナリオ実行中 When タスクがエラー終了する Then 該当階層の階層実行ステータスが Error になる |
| SPEC-003-03 | | プロセスを setup → pre_execute → execute → post_execute → teardown の順で実行し、scripts プラグインでは scripts/ 配下をファイル名昇順に逐次実行する。エラー発生時は後続スクリプトを実行せず Blocked として記録しエラー終了する（ステップ実行ステータス: Pending → Success / Error / Blocked）。これらを内蔵ランナーが従来と同一仕様で提供する | | 1. Given 3 つのスクリプトを昇順配置 When 2 番目がエラー終了する Then 3 番目は実行されず Blocked と記録されプロセスは Error になる<br>2. Given 全スクリプトが正常終了 When プロセスを実行する Then 全ステップが Success となりプロセスは Success になる |
| REQ-004 | テスト実行者が、ワークフローエンジン（digdag server）の起動・停止・再起動・状態確認を行い、実行基盤を制御できる | | 廃止: 実行エンジンの内包化（Go 単一バイナリへの逐次実行ランナー内蔵）により外部ワークフローエンジン digdag が廃止され、実行基盤（digdag server）を制御するという要求自体が消滅した。シナリオ実行は stfw run の 1 コマンドで完結する（REQ-003） | |
| SPEC-004-01 | | 廃止: server start / stop / restart / status による digdag server 制御、pid ファイル管理・多重起動禁止、状態 DB（--memory / --database）・max-task-threads 設定、server 稼働状態。stfw.yml の stfw.server.* 設定は読み込み時に廃止警告を表示する | | 1. Given v1.0 の stfw When stfw server の各サブコマンドを実行する Then コマンドは存在しない<br>2. Given stfw.server.* 設定を含む stfw.yml When 設定を読み込む Then 廃止警告が表示され設定は無視される |
| SPEC-004-02 | | 廃止: stfw digdag による digdag CLI のラップ実行（digdag 自体が存在しないため） | | 1. Given v1.0 の stfw When stfw digdag を実行する Then コマンドは存在しない |
| REQ-005 | テスト結果確認者が、run / scenario / bizdate / process 各階層の実行開始・終了（成功 / 失敗）を webhook 通知として外部システムで受け取れる | | 廃止: 受信側の実装・保守が必要で、可視化・分析には別途仕組みが要るため。OTel トレースによる実行結果通知（REQ-009）に置換し、webhook 通知機能は完全に廃止する（併存させない） | |
| SPEC-005-01 | | 廃止: 各階層の start / end 時の payload JSON 変換と設定済み URL 群への非同期 HTTP POST。payload JSON スキーマの互換維持要求も廃止する（REQ-009 の OTLP トレースエクスポートに置換） | | 1. Given webhook 機能の廃止後 When 任意の階層が開始・終了する Then HTTP POST による webhook 通知は送信されない |
| SPEC-005-02 | | 廃止: on_start / on_success / on_error 設定によるイベント種別ごとの通知抑制（stfw.yml の stfw.webhooks.* 設定ごと廃止。OTel 側に同等の抑制設定は設けない） | | 1. Given webhook 機能の廃止後 When stfw.yml に stfw.webhooks.* を記述する Then 設定は参照されず通知の挙動に影響しない |
| REQ-006 | テスト結果確認者が、実行ジャーナルをソースとする stfw status / stfw report（静的 HTML レポート）とログファイルにより実行状況と結果を確認し、失敗時の調査ができる | | digdag 廃止により Web UI・ログ追従（run -f）による確認手段が消滅したため、実行結果の唯一のソースである実行ジャーナル（journal.jsonl）のリプレイに基づく確認手段（status / report）へ置換する。進捗把握と障害調査ができることは従来どおり必要である | |
| SPEC-006-01 | | 実行結果の唯一のソースは実行ジャーナル（.stfw/runs/{run_id}/journal.jsonl。追記専用 JSONL、node_start / steps_enumerated / step_end / node_end イベント）とする。OTel トレース（REQ-009）もジャーナルイベントの投影である | | 1. Given シナリオ実行完了 When .stfw/runs/{run_id}/journal.jsonl を参照する Then 全階層・ステップの開始・終了イベントが追記専用 JSONL として記録されている |
| SPEC-006-02 | | stfw status [run_id] で実行ジャーナルをリプレイし、階層ツリー + ステータスを表示する。digdag Web UI・ログ追従（run -f）・attempt URL 案内による確認は廃止する | | 1. Given シナリオ実行中または実行完了 When stfw status を実行する Then 階層ツリーと各階層・ステップのステータスが表示される<br>2. Given v1.0 の stfw When run コマンドのオプションを確認する Then ログ追従オプション（-f / --follow）は存在しない |
| SPEC-006-03 | | stfw report [run_id] で静的 HTML レポート（.stfw/reports/ に index + run 詳細）を生成する。実行中も process 終了ごとに増分再生成され、準リアルタイムに閲覧できる。Docker Compose 構成では nginx サービスが reports 共有 volume を配信し、ブラウザ（http://localhost:8080）でレポートを閲覧できる | | 1. Given シナリオ実行完了 When stfw report を実行する Then .stfw/reports/ に index と run 詳細の静的 HTML が生成される<br>2. Given シナリオ実行中 When process が終了する Then レポートが増分再生成され準リアルタイムに閲覧できる<br>3. Given Docker Compose 構成で起動済み When ブラウザで http://localhost:8080 を開く Then nginx が配信するレポートを閲覧できる |
| SPEC-006-04 | | ログレベル（trace / debug / info / warn / error）の設定・変更、.stfw/stfw.log への日次ローテーション集約、terminal 実行時のカラー出力、環境変数 PASSWORD / TOKEN 値の [secret] マスキングは従来仕様を維持する | | 1. Given PASSWORD 環境変数を含む処理 When ログへ出力される Then 値が [secret] に置換されている<br>2. Given loglevel を debug に設定 When コマンドを実行する Then debug レベルのログが出力される |
| REQ-007 | 環境管理者が、環境別 inventory によるテスト対象ホストのグループ管理、資格情報の暗号化保管・参照と旧形式からの移行、SSH サーバキーの一括登録を行える | | リモートホストへのテスト適用に伴い資格情報の平文保管を避ける必要があることは変わらない。v1.0 では外部 openssl 依存をなくすため暗号化方式を age (X25519) に刷新し、旧形式からの移行手段の提供と、旧実装では未配線だった SSH known_hosts 登録の正式コマンド化を行った | |
| SPEC-007-01 | | 環境単位の inventory ファイルにホストグループ（web / ap / db 等）を定義し、stfw inventory exists でグループ存在確認、stfw inventory list でホスト一覧取得ができる（旧 --is-exist / --list と出力互換）。グループ名 all は全グループ横断の予約値とする（従来仕様を維持） | | 1. Given inventory にグループ web を定義 When stfw inventory list web を実行する Then 旧 --list と互換の出力で web グループのホスト一覧が返る<br>2. Given inventory に複数グループを定義 When グループ名 all を指定する Then 全グループのホストが対象になる |
| SPEC-007-02 | | 資格情報コマンドを stfw secret keygen / set / show に再編する（旧 gen-encrypt-key / passwd / passwd --show）。暗号化方式は openssl RSA + S/MIME(AES256) から age (X25519) に変更する。鍵再生成抑止・同一ホスト×ユーザーの重複登録禁止・ログのシークレットマスキングは同一仕様で維持する | | 1. Given stfw secret keygen でキー生成済み When stfw secret set host user pass を実行する Then age (X25519) で暗号化された資格情報ファイルが生成される<br>2. Given 同一ホスト×ユーザー登録済み When stfw secret set で再登録する Then エラー終了する<br>3. Given キー生成済み When --force なしで stfw secret keygen を実行する Then 再生成は拒否される |
| SPEC-007-03 | | stfw secret migrate を新設する。旧 S/MIME 形式を読み込み専用でサポートし、age 形式へ一括変換する（旧ファイルは .bak 退避） | | 1. Given 旧 S/MIME 形式の資格情報ファイルが存在 When stfw secret migrate を実行する Then 全ファイルが age 形式に変換され、旧ファイルは .bak として退避される<br>2. Given 旧 S/MIME 形式の資格情報ファイルが存在 When 移行前に参照する Then 読み込み専用でサポートされ復号参照できる |
| SPEC-007-04 | | テスト対象ホストの SSH サーバキーの known_hosts 登録を stfw ssh trust <host\|group> として正式コマンド化する。inventory グループ指定で一括登録できる（旧実装では未配線だった機能の配線） | | 1. Given テスト対象ホスト名 When stfw ssh trust <host> を実行する Then known_hosts から旧キーが削除され新しいサーバキーが登録される<br>2. Given inventory にグループ定義済み When stfw ssh trust <group> を実行する Then グループ内全ホストのサーバキーが一括登録される |
| REQ-008 | シナリオ作成者が、プロセスタイプをプラグインとして追加・拡張できる（同梱プラグインは scripts のみ） | | テスト対象固有のプロセス種別への対応のための拡張機構であることは変わらない。v1.0 では CLI 体系の刷新に合わせてコマンドを再編し、解決順・env 契約は互換を維持する | |
| SPEC-008-01 | | プラグイン操作を stfw plugin list / stfw plugin install に再編する（旧 process -l / -I。解決順互換）。scaffold 生成は stfw new process で行う。同名プラグインはプロジェクト（{proj}/plugins/）→ 組込みの順に解決されプロジェクト側が優先される（従来仕様を維持。組込みの配置は STFW_HOME 廃止に伴い配布物側に変更）。プラグイン env 契約（stfw_* / STFW_PROJ_DIR 系）は維持する | | 1. Given 標準構成 When stfw plugin list を実行する Then 同梱プラグイン scripts が一覧表示される<br>2. Given プロジェクトに同名プラグインを配置 When プロセスタイプを解決する Then 従来と互換の解決順でプロジェクト側プラグインが優先される |
| REQ-009 | テスト結果確認者が、run / scenario / bizdate / process / step の実行状況を OTLP トレース（スパンツリー）として既存のオブザーバビリティ基盤（Jaeger / Grafana Tempo / Datadog 等）で受け取り、そのまま可視化・分析できる | | 現行 webhook 通知は受信側の実装・保守が必要で、可視化・分析には別途仕組みが要る。OpenTelemetry の標準プロトコル（OTLP）でトレースを送信すれば、既存のオブザーバビリティ基盤でそのまま可視化・分析できるため | |
| SPEC-009-01 | | 階層実行をスパンツリーにマップして OTLP トレースエクスポートを実装する。run = ルートスパン、scenario / bizdate / process = 子スパン、step = 末端スパンとする。シグナルはトレースのみとし、ログ・メトリクスは送らない。スパンの開始・終了時刻は実行ジャーナル（journal.jsonl）のイベント時刻と一致させ、実行ジャーナルが唯一のソースであるという現行設計を維持して OTel エクスポートはジャーナルイベントの投影として実装する | | 1. Given OTLP エクスポート先設定済み When シナリオを一括自動実行する Then run をルートとし scenario / bizdate / process / step を子孫とするスパンツリーが OTLP 受信先に記録される<br>2. Given OTLP エクスポート先設定済み When 実行が完了する Then 各スパンの開始・終了時刻が実行ジャーナル（journal.jsonl）のイベント時刻と一致する<br>3. Given OTLP エクスポート先設定済み When 実行が完了する Then トレース以外のシグナル（ログ・メトリクス）は送信されない |
| SPEC-009-02 | | スパン属性に実行コンテキスト（run_id、階層タイプ、bizdate、seq、group、プロセスタイプ、終了コード等。旧 webhook payload が持っていた情報を引き継ぐ）を載せる。実行ステータス Error はスパンステータス Error にマップし、Blocked ステップはスパン属性で表現する | | 1. Given OTLP エクスポート先設定済み When プロセスを実行する Then 各スパンの属性に run_id・階層タイプ・bizdate・seq・group・プロセスタイプ・終了コード等の実行コンテキストが記録される<br>2. Given OTLP エクスポート先設定済み When タスクがエラー終了する Then 該当スパンのスパンステータスが Error になる<br>3. Given OTLP エクスポート先設定済み When 先行スクリプトのエラーで後続ステップが Blocked になる Then 該当 step スパンの属性で Blocked が表現される |
| SPEC-009-03 | | エクスポート先は OTel 標準の環境変数（OTEL_EXPORTER_OTLP_ENDPOINT 等）を尊重し、stfw.yml でも設定できる（stfw.otel.endpoint 等）。送信先が未設定の場合は送信しない（現行 webhook の「URL 未設定なら送信しない」と同じ方針） | | 1. Given 環境変数 OTEL_EXPORTER_OTLP_ENDPOINT を設定 When シナリオを実行する Then その endpoint へトレースが送信される<br>2. Given stfw.yml の stfw.otel.endpoint を設定 When シナリオを実行する Then その endpoint へトレースが送信される<br>3. Given 環境変数・stfw.yml のいずれにも送信先が未設定 When シナリオを実行する Then トレースは送信されない |
| SPEC-009-04 | | トレースの送信失敗は実行を失敗させず、ログ警告のみとする（現行 webhook と同じ方針） | | 1. Given OTLP エクスポート先が到達不能 When シナリオを実行する Then 実行は失敗せず完走し、実行ログに警告が記録される |
| REQ-010 | webhook 通知機能を完全に廃止する（OTel トレースエクスポートと併存させない） | | 通知手段を OTLP トレースに一本化するため。webhook を併存させると受信側の実装・保守と payload JSON スキーマの互換維持が残り続け、置換の目的（保守負担の解消と既存オブザーバビリティ基盤の活用）を達成できないため | |
| SPEC-010-01 | | webhook 送信機能を撤去し、stfw.yml の stfw.webhooks.* 設定（urls / on_start / on_success / on_error）を廃止する。webhook payload JSON スキーマの互換維持要求も廃止する | | 1. Given 廃止後の stfw When 任意の階層・ステップが開始・終了する Then webhook（HTTP POST）は一切送信されない<br>2. Given 廃止後の stfw のテンプレート When stfw init でプロジェクトを初期化する Then 生成される stfw.yml に stfw.webhooks.* 設定は含まれない |
| REQ-011 | 開発者が、フレームワーク自体の品質を Go 標準のテスト・静的解析と GitHub Actions の CI/CD で継続的に保証できる | | bash 実装向けの検証基盤（shunit2 / serverspec / kcov / shellcheck / Travis CI）は Go 移行後の実装を検証できない。保守性を確保するには Go 対応のテスト・lint と、配布（goreleaser / ghcr.io）まで一貫した現行 CI/CD 基盤への刷新が必要なため | |
| SPEC-011-01 | | フレームワーク自体の検証を Go の単体テスト + testscript 受け入れテスト + golangci-lint に変更する（shunit2 / serverspec / kcov / shellcheck / Travis CI は廃止） | | 1. Given リポジトリのソースコード When go test と golangci-lint を実行する Then 単体テスト・testscript 受け入れテスト・lint が実行できる<br>2. Given v1.0 のリポジトリ When 検証基盤を確認する Then shunit2 / serverspec / kcov / shellcheck / Travis CI の構成は存在しない |
| SPEC-011-02 | | CI/CD は GitHub Actions で行う。PR で lint + test、master マージで snapshot ビルド、tag 付与で goreleaser によるリリースと ghcr.io への配布を実行する | | 1. Given PR 作成 When CI が実行される Then lint と test が実行される<br>2. Given master へのマージ When CI が実行される Then snapshot ビルドが実行される<br>3. Given tag 付与 When CI が実行される Then goreleaser によるリリース成果物の公開と ghcr.io への Docker image 配布が実行される |
