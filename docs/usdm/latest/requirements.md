# USDM 要求仕様書

- システム名: stfw
- イベントID: 20260707_233136_harvest_initial
- 作成日時: 2026-07-08T00:10:32
- ソース: 逆生成: /Users/suwa_sh/src/github.com/scenario-test-framework/stfw (commit ed02ba61, branch feature/kcov) を dist-harvest で解析

| ID | 要求 | 仕様 | 理由（背景） | 説明 |
|----|------|------|------------|------|
| REQ-001 | テスト担当者が、tar.gz 展開とテンプレート初期化のみでシナリオテスト実行基盤（stfw プロジェクト）を各ホストに用意できる | | Getting Started がダウンロード→展開→install→init を最短導入手順として一次記述しており、導入の容易さが利用開始の前提となっているため | |
| SPEC-001-01 | | 配布アーカイブ（stfw-with-depends-*.tar.gz）を展開して install を実行すると、依存モジュール（digdag jar 等）を配布元からダウンロードして実行基盤を構成する | | 1. Given アーカイブを展開済み When bin/install を実行する Then 依存モジュールが modules/ に配置され stfw コマンドが利用可能になる |
| SPEC-001-02 | | stfw init でテンプレート一式（stfw.yml / config / sample シナリオ）を展開してプロジェクトを開始できる。stfw.yml が既に存在するディレクトリでは再初期化をエラーとする | | 1. Given 空のプロジェクトディレクトリ When stfw init を実行する Then stfw.yml・config/・scenario/sample が生成される<br>2. Given stfw.yml が存在するディレクトリ When stfw init を実行する Then エラー終了する |
| SPEC-001-03 | | プロジェクト設定（stfw.yml）はデフォルト（STFW_HOME/config）→ プロジェクトの順に読込・上書きされ、環境変数として全スクリプトへ公開される（webhook URL の環境変数参照等が可能） | | 1. Given プロジェクトの stfw.yml でデフォルト設定値を上書きした状態 When 任意のコマンドを実行する Then プロジェクト側の値が環境変数として有効になる |
| REQ-002 | シナリオ作成者が、業務日付（bizdate）をまたぐ一連の業務処理を scenario > bizdate > process の 3 階層ディレクトリ構造とスクリプトとして記述し、ワークフロー定義を自動生成・検証できる | | 業務日付の進行を伴うバッチ処理系業務システムのシナリオテストでは、日付単位でテストを区切って進行させる必要があるため（推測: ドメイン一般論。理由の一次記述は存在しない） | |
| SPEC-002-01 | | scenario -i / bizdate -i / process -i で 3 階層の scaffold（ディレクトリ・metadata.yml・config・scripts 雛形）を生成できる。bizdate は YYYYMMDD の 8 桁数字、seq は数値のみ、group は「_」を含まないことを検証する | | 1. Given プロジェクト初期化済み When stfw scenario -i / bizdate -i / process -i を順に実行する Then scenario/{name}/_{seq}_{bizdate}/_{seq}_{group}_{type}/ の階層が生成される<br>2. Given bizdate scaffold 生成 When bizdate に 8 桁数字以外を指定する Then エラー終了する |
| SPEC-002-02 | | プロセス配下の scripts/ に任意言語の実行可能ファイルをファイル名昇順 = 実行順で配置し、Process/Plugin 設定（config.yml）の値を環境変数として全スクリプトへ公開できる（組込み → プロジェクト → シナリオ内 Process 設定の順に上書き） | | 1. Given config.yml に stfw.process.{type} 配下のキーを定義 When プロセスを実行する Then 全スクリプトから環境変数として参照できる |
| SPEC-002-03 | | ディレクトリ構造（「_」始まりディレクトリの名前昇順）からワークフロー定義（scenario.dig / bizdate.dig）を自動生成できる。cascade 指定で配下の bizdate.dig まで連鎖生成できる | | 1. Given 3 階層 scaffold 作成済み When stfw scenario -G を実行する Then scenario.dig と配下の bizdate.dig が生成される |
| SPEC-002-04 | | dry-run モード（stfw run -d / stfw process -d）で、実タスク（execute / post_execute）を実行せず setup → pre_execute → teardown のみでワークフローを検証できる | | 1. Given ワークフロー定義生成済み When stfw run -d を実行する Then execute / post_execute は実行されず setup・pre_execute・teardown のみ実行される |
| REQ-003 | テスト実行者が、ディレクトリ構造で記述したシナリオテスト（複数シナリオ）をワークフローエンジン digdag で一括自動実行でき、実行順序の保証とエラー時停止が担保される | | 初回コミットの機能名 run workflow according to directory structure が示すとおり、ディレクトリ構造どおりのワークフロー自動実行がフレームワークの中核目的であるため | |
| SPEC-003-01 | | stfw run <scenario-names...> で run_id（_{YYYYMMDDHHMMSS}_{PID}）を採番し、digdag プロジェクトを push して実行を開始し、attempt_id を実行コンテキストに保存する。digdag server が起動中（pid 生存）かつ対象シナリオが存在することを前提条件とする | | 1. Given digdag server 起動中かつシナリオ存在 When stfw run を実行する Then run_id が採番され attempt_id が実行コンテキストに保存される<br>2. Given digdag server 停止中 When stfw run を実行する Then 前提条件エラーで終了する |
| SPEC-003-02 | | digdag からの呼び戻し（sh> オペレータ）で run / scenario / bizdate 各階層の setup / teardown を実行し、階層実行ステータスを Started → Success（正常経路）/ Error（_error 経路）と確定させる | | 1. Given シナリオ実行中 When 全タスクが正常終了する Then 各階層の teardown で階層実行ステータスが Success になる<br>2. Given シナリオ実行中 When タスクがエラー終了する Then _error 経路の teardown で階層実行ステータスが Error になる |
| SPEC-003-03 | | プロセスを setup → pre_execute → execute → post_execute → teardown の順で実行し、scripts プラグインでは scripts/ 配下をファイル名昇順に逐次実行する。エラー発生時は後続スクリプトを実行せず Blocked として記録しエラー終了する（ステップ実行ステータス: Pending → Success / Error / Blocked）。プロセスタイプのプラグインがインストール済みであることを前提とする | | 1. Given 3 つのスクリプトを昇順配置 When 2 番目がエラー終了する Then 3 番目は実行されず Blocked と記録されプロセスは Error になる<br>2. Given 全スクリプトが正常終了 When プロセスを実行する Then 全ステップが Success となりプロセスは Success になる |
| REQ-004 | テスト実行者が、ワークフローエンジン（digdag server）の起動・停止・再起動・状態確認を行い、実行基盤を制御できる | | シナリオ実行は digdag server の起動が前提条件であり、実行基盤の制御手段が無ければシナリオを実行できないため（実装上の前提条件からの導出） | |
| SPEC-004-01 | | server start / stop / restart / status で digdag server を制御できる。bind・port・状態 DB（--memory / --database）・max-task-threads を指定でき、pid ファイルで管理して多重起動を禁止する（server 稼働状態: 停止中 ⇔ 起動中） | | 1. Given server 停止中 When stfw server start を実行する Then pid ファイルが作成され起動中になる<br>2. Given server 起動中 When stfw server start を実行する Then 多重起動エラーになる<br>3. Given server 起動中 When stfw server stop を実行する Then SIGTERM で停止し pid ファイルが削除される |
| SPEC-004-02 | | stfw digdag で digdag CLI を stfw の環境設定（PATH_DIGDAG 等）込みでラップ実行できる（トラブルシュート・詳細操作用の補助コマンド） | | 1. Given stfw プロジェクト内 When stfw digdag <args> を実行する Then stfw の環境設定込みで digdag CLI が実行される |
| REQ-005 | テスト結果確認者が、run / scenario / bizdate / process 各階層の実行開始・終了（成功 / 失敗）を webhook 通知として外部システムで受け取れる | | payload に進捗・所要時間・digdag URL を含むことから、実行状況の外部監視・可視化のためと推測（コミットメッセージは「webhook機能を追加」のみで目的の明文なし） | |
| SPEC-005-01 | | 各階層の start / end 時に payload（id・parent_id・type・status・時刻・処理時間・stfw / digdag / project 情報）を JSON 変換し、設定済み URL 群へ非同期 HTTP POST する。payload は run > scenario > bizdate > process の親子ツリーを構成し、process の payload はステップ別実行結果（plugin.targets）を含む | | 1. Given webhook URL 設定済み When プロセスが開始する Then status=Started の payload が設定 URL 群へ POST される<br>2. Given webhook URL 設定済み When プロセスがエラー終了する Then status=Error とステップ別結果を含む payload が POST される |
| SPEC-005-02 | | webhook 通知は on_start / on_success / on_error 設定（true / false）でイベント種別ごとに抑制できる | | 1. Given on_start=false When 階層が開始する Then start 通知は送信されない<br>2. Given on_success=false When 階層が正常終了する Then end（Success）通知は送信されない |
| REQ-006 | テスト結果確認者が、実行ログの追従表示・ログファイル・digdag Web UI により実行状況と結果を確認し、失敗時の調査ができる | | 確認手順の明文ドキュメントは無く、実装された確認手段（--follow・attempt URL 案内・ログ集約とマスキング）から進捗把握と障害調査のためと推測 | |
| SPEC-006-01 | | stfw run -f（--follow）で実行中 attempt のログを終了までリアルタイム追従表示し、最終 state を判定する（success 以外はエラー扱い） | | 1. Given シナリオ実行開始済み When stfw run -f で実行する Then attempt 終了までログが追従表示され最終 state が判定される |
| SPEC-006-02 | | ログレベル（trace / debug / info / warn / error）を設定・オプションで変更でき、ログは .stfw/stfw.log へ日次ローテーションで集約される。terminal 実行時はカラー出力し、環境変数 PASSWORD / TOKEN の値は [secret] にマスキングする | | 1. Given PASSWORD 環境変数を含む処理 When ログへ出力される Then 値が [secret] に置換されている<br>2. Given loglevel を debug に設定 When コマンドを実行する Then debug レベルのログが出力される |
| SPEC-006-03 | | 実行開始時に digdag Web UI の attempt URL（http://{ip}:{port}/attempts/{attempt_id}）を案内し、実行状況・詳細を Web UI で確認できる | | 1. Given シナリオ実行開始 When stfw run が完了する Then attempt URL が出力され Web UI で実行詳細を確認できる |
| REQ-007 | 環境管理者が、環境別 inventory によるテスト対象ホストのグループ管理と、資格情報（パスワード）の暗号化保管・参照を行える | | リモートホストへのプロセス適用（feature/remote_process）に伴い資格情報の平文保管を避けるためと推測（PR #5 passwd → PR #7 remote_process のコミット順序と機能の組み合わせからの導出） | |
| SPEC-007-01 | | 環境単位の inventory ファイル（例: staging.yml）にホストグループ（web / ap / db 等）を定義し、--is-exist でグループ存在確認、--list でホスト一覧取得ができる。グループ名 all は全グループ横断の予約値とする | | 1. Given inventory にグループ web を定義 When stfw inventory --list web を実行する Then web グループのホスト一覧が返る<br>2. Given inventory に複数グループを定義 When グループ名 all を指定する Then 全グループのホストが対象になる |
| SPEC-007-02 | | gen-encrypt-key で RSA 2048 の暗号化キーペアを生成し（既存時は --force 指定でのみ再生成）、passwd <host> <user> <password> で S/MIME(AES256) により暗号化登録、--show で復号参照できる。同一ホスト×ユーザーの重複登録は不可とする | | 1. Given キーペア生成済み When stfw passwd host user pass を実行する Then config/passwd/ に暗号化ファイル {host}-{user} が生成される<br>2. Given 同一ホスト×ユーザー登録済み When 再登録する Then エラー終了する<br>3. Given キーペア生成済み When --force なしで gen-encrypt-key を実行する Then 再生成は拒否される |
| SPEC-007-03 | | テスト対象ホストの SSH サーバキーを known_hosts へ登録する機能（ssh-keygen -R による旧キー削除 + ssh-keyscan による再登録）を提供する（ユーザースクリプトからの利用想定・呼出経路未確定の候補仕様） | | 1. Given テスト対象ホスト名 When gen_ssh_server_key を実行する Then known_hosts から旧キーが削除され新しいサーバキーが登録される |
| REQ-008 | シナリオ作成者が、プロセスタイプをプラグインとして追加・拡張できる（同梱プラグインは scripts のみ） | | 「プロセスを追加できるように、既存のプロセスを整理」というコミットが直接の証拠だが、拡張が必要になる具体的ユースケースの明文は無い（テスト対象固有のプロセス種別への対応のためと推測） | |
| SPEC-008-01 | | stfw process -l でプラグイン一覧、-I <process-type> で依存モジュールのインストール、-i で scaffold 生成ができる。同名プラグインはプロジェクト（{proj}/plugins/）→ 組込み（STFW_HOME/plugins/）の順に解決され、プロジェクト側が優先される | | 1. Given 標準構成 When stfw process -l を実行する Then 同梱プラグイン scripts が一覧表示される<br>2. Given プロジェクトに同名プラグインを配置 When プロセスタイプを解決する Then プロジェクト側プラグインが優先される |
